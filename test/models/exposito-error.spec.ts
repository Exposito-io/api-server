import * as chai from 'chai'
import { ExpositoError } from '../../models/exposito-error'


describe("Hello", () => {
    let error: ExpositoError

    beforeEach(() => {
        //error = new ExpositoError()
    })

    it("should render correctly", async () => {
        chai.assert.strictEqual(1, 1)
    })

})